# Configuración de Base de Datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rrhh_sistema
DB_USER=postgres
DB_PASSWORD=tu_password

# Configuración de Email (Gmail)
EMAIL_USER=sistemas@tuempresa.com
EMAIL_PASS=tu_contraseña_de_aplicacion_gmail
EMAIL_SISTEMAS=sistemas@tuempresa.com
EMAIL_RRHH=rrhh@tuempresa.com

# URL del Frontend (para enlaces en emails)
FRONTEND_URL=http://localhost:3001

# Puerto del servidor
PORT=3000

# Configuración JWT (si usas autenticación)
JWT_SECRET=tu_jwt_secret_aqui

# Configuración de Email Alternativa (si no usas Gmail)
# SMTP_HOST=smtp.tudominio.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=noreply@tudominio.com
# SMTP_PASS=password_smtp
